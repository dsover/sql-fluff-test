# name: Bump version v2
# on:
#   workflow_dispatch:
#     inputs:
#       versionType:
#         description: 'The type of version bump to perform: major, minor, patch'
#         required: true
#         default: 'patch'
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@master
#       - name: Bump version and push tag
#         uses: hennejg/github-tag-action@v4.3.1
#         with:
#           github_token: ${{ secrets.PAT }}
#           default_bump: ${{ github.event.inputs.versionType }} 


#           # example 2: on merge to master from pull request (recommended)
name: Bump version
on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-22.04
    # permissions:
    #   contents: write
    steps:
    - uses: actions/checkout@v3
      with:
        ref: ${{ github.event.pull_request.merge_commit_sha }}
        fetch-depth: '0'

    - name: Bump version and push tag
      uses: anothrNick/github-tag-action@1.67.0      # Don't use @master or @v1 unless you're happy to test the latest version
      env:
        GITHUB_TOKEN: ${{ secrets.PAT }} # if you don't want to set write permissions use a PAT token
        WITH_V: true
        # PRERELEASE: true